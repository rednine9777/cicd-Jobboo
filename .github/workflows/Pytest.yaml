name: Pytest

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_run:
    workflows: ["pip-install"]
    types:
      - completed
  

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Run pytest
      run: pytest -s app/src/test/